import {vec2, vec3} from 'gl-matrix';

class Limb {
  jointPos: number[];
  jointRadii: number[];

  constructor() {
  }

  generate(startPos: vec3) {
    
  }

  animate(time: number) {
    
  }
};

export default Limb;